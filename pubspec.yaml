name: recipes
description: Recipe Finder

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  cached_network_image: ^3.1.0 # Download and cache the images you’ll use in the app.
  flutter_slidable: ^0.6.0  # Build a widget that lets the user slide a card left and right to
#perform different actions, like deleting a saved recipe.
  flutter_svg: ^0.22.0 #  Load SVG images without the need to use a program to convert them
#to vector files.
  shared_preferences: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/recipes1.json
    - assets/recipes2.json
    - assets/images/
